#include "main.h"

/**
 * _abs- computes the absolute value of an integer
 * @u:  computes the absolute value of an integer.
 * Return: Absolute value of number or zero
 */

int _abs(int u)
{
	if (u >= 0)
	{
		return (u);
	}
	return (-u);
}
