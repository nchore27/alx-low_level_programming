#include "main.h"

/**
 * add- function that adds two integers and returns the result
 * @h: number to be added
 * @i: number to be added
 * Return: Always 0
 */
int add(int h, int i)
{
	return (h + i);
}
